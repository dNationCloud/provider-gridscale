---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: servers.gridscale.crossplane.io
spec:
  group: gridscale.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - gridscale
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Server is the Schema for the Servers API. Manages a server in
          gridscale.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerSpec defines the desired state of Server
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  autoRecovery:
                    description: |-
                      If the server should be auto-started in case of a failure (default=true).
                      If the server should be auto-started in case of a failure (default=true).
                    type: boolean
                  availabilityZone:
                    description: |-
                      Defines which Availability-Zone the Server is placed.
                      Defines which Availability-Zone the Server is placed.
                    type: string
                  cores:
                    description: |-
                      The number of server cores.
                      The number of server cores.
                    type: number
                  hardwareProfile:
                    description: |-
                      The hardware profile of the Server. Options are default, legacy, nested, cisco_csr, sophos_utm, f5_bigip and q35 at the moment of writing. If it is not set, the backend will set it by default. Check the official docs.
                      Specifies the hardware settings for the virtual machine. Note: hardware_profile and hardware_profile_config parameters can't be used at the same time.
                    type: string
                  hardwareProfileConfig:
                    description: |-
                      Specifies the custom hardware settings for the virtual machine. Note: hardware_profile and hardware_profile_config parameters can't be used at the same time. Note: If hardware_profile_config is set, all fields of hardware_profile_config MUST be set.
                      Specifies the custom hardware settings for the virtual machine. Note: hardware_profile and hardware_profile_config parameters can't be used at the same time.
                    items:
                      properties:
                        hypervExtensions:
                          description: Boolean.
                          type: boolean
                        machinetype:
                          description: 'Allowed values: "i440fx", "q35_bios", "q35_uefi".'
                          type: string
                        nestedVirtualization:
                          description: Boolean.
                          type: boolean
                        networkModel:
                          description: 'Allowed values: "e1000", "e1000e", "virtio",
                            "vmxnet3".'
                          type: string
                        serialInterface:
                          description: Boolean.
                          type: boolean
                        serverRenice:
                          description: Boolean.
                          type: boolean
                        storageDevice:
                          description: 'Allowed values: "ide", "sata", "virtio_scsi",
                            "virtio_block".'
                          type: string
                        usbController:
                          description: 'Allowed values: "nec_xhci", "piix3_uhci".'
                          type: string
                      type: object
                    type: array
                  ipv4:
                    description: 'The UUID of the IPv4 address of the server. (***NOTE:
                      The server will NOT automatically be connected to the public
                      network; to give it access to the internet, please add server
                      to the public network.)'
                    type: string
                  ipv4Ref:
                    description: Reference to a IPv4 in gridscale to populate ipv4.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  ipv4Selector:
                    description: Selector for a IPv4 in gridscale to populate ipv4.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  ipv6:
                    description: 'The UUID of the IPv6 address of the server. (***NOTE:
                      The server will NOT automatically be connected to the public
                      network; to give it access to the internet, please add server
                      to the public network.)'
                    type: string
                  ipv6Ref:
                    description: Reference to a IPv6 in gridscale to populate ipv6.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  ipv6Selector:
                    description: Selector for a IPv6 in gridscale to populate ipv6.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isoimage:
                    description: The UUID of an ISO image in gridscale. The server
                      will automatically boot from the ISO if one was added. The UUIDs
                      of ISO images can be found in the expert panel.
                    type: string
                  labels:
                    description: |-
                      List of labels in the format [ "label1", "label2" ].
                      List of labels.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  memory:
                    description: |-
                      The amount of server memory in GB.
                      The amount of server memory in GB.
                    type: number
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters
                    type: string
                  network:
                    description: Connects a network to the server. The network ordering
                      of the server corresponds to the order of the networks in the
                      server resource block.
                    items:
                      properties:
                        bootdevice:
                          description: Make this network the boot device. This can
                            only be set for one network.
                          type: boolean
                        firewallTemplateUuid:
                          description: The UUID of firewall template.
                          type: string
                        firewallTemplateUuidRef:
                          description: Reference to a Firewall in gridscale to populate
                            firewallTemplateUuid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        firewallTemplateUuidSelector:
                          description: Selector for a Firewall in gridscale to populate
                            firewallTemplateUuid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        ip:
                          description: |-
                            Manually assign DHCP IP to the server (if applicable).
                            Manually assign DHCP IP to the server.
                          type: string
                        objectUuid:
                          description: The object UUID or id of the network.
                          type: string
                        objectUuidRef:
                          description: Reference to a Network in gridscale to populate
                            objectUuid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        objectUuidSelector:
                          description: Selector for a Network in gridscale to populate
                            objectUuid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        ordering:
                          description: DEPRECATED  Defines the ordering of the network
                            interfaces. Lower numbers have lower PCI-IDs.
                          type: number
                        rulesV4In:
                          description: Firewall template rules for inbound traffic
                            - covers ipv4 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                        rulesV4Out:
                          description: Firewall template rules for outbound traffic
                            - covers ipv4 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                        rulesV6In:
                          description: Firewall template rules for inbound traffic
                            - covers ipv6 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                        rulesV6Out:
                          description: Firewall template rules for outbound traffic
                            - covers ipv6 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  power:
                    description: |-
                      The power state of the server. Set this to true to will boot the server, false will shut it down.
                      The number of server cores.
                    type: boolean
                  storage:
                    description: |-
                      Connects a storage to the server. **NOTE: The first storage is always the boot device.
                      A list of storages attached to the server. The first storage in the list is always set as the boot storage of the server.
                    items:
                      properties:
                        objectUuid:
                          description: The object UUID or id of the storage.
                          type: string
                        objectUuidRef:
                          description: Reference to a Storage in gridscale to populate
                            objectUuid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        objectUuidSelector:
                          description: Selector for a Storage in gridscale to populate
                            objectUuid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  userDataBase64:
                    description: |-
                      For system configuration on first boot. May contain cloud-config data or shell scripting, encoded as base64 string. Supported tools are cloud-init, Cloudbase-init, and Ignition.
                      For system configuration on first boot. May contain cloud-config data or shell scripting, encoded as base64 string. Supported tools are cloud-init, Cloudbase-init, and Ignition.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoRecovery:
                    description: |-
                      If the server should be auto-started in case of a failure (default=true).
                      If the server should be auto-started in case of a failure (default=true).
                    type: boolean
                  availabilityZone:
                    description: |-
                      Defines which Availability-Zone the Server is placed.
                      Defines which Availability-Zone the Server is placed.
                    type: string
                  cores:
                    description: |-
                      The number of server cores.
                      The number of server cores.
                    type: number
                  hardwareProfile:
                    description: |-
                      The hardware profile of the Server. Options are default, legacy, nested, cisco_csr, sophos_utm, f5_bigip and q35 at the moment of writing. If it is not set, the backend will set it by default. Check the official docs.
                      Specifies the hardware settings for the virtual machine. Note: hardware_profile and hardware_profile_config parameters can't be used at the same time.
                    type: string
                  hardwareProfileConfig:
                    description: |-
                      Specifies the custom hardware settings for the virtual machine. Note: hardware_profile and hardware_profile_config parameters can't be used at the same time. Note: If hardware_profile_config is set, all fields of hardware_profile_config MUST be set.
                      Specifies the custom hardware settings for the virtual machine. Note: hardware_profile and hardware_profile_config parameters can't be used at the same time.
                    items:
                      properties:
                        hypervExtensions:
                          description: Boolean.
                          type: boolean
                        machinetype:
                          description: 'Allowed values: "i440fx", "q35_bios", "q35_uefi".'
                          type: string
                        nestedVirtualization:
                          description: Boolean.
                          type: boolean
                        networkModel:
                          description: 'Allowed values: "e1000", "e1000e", "virtio",
                            "vmxnet3".'
                          type: string
                        serialInterface:
                          description: Boolean.
                          type: boolean
                        serverRenice:
                          description: Boolean.
                          type: boolean
                        storageDevice:
                          description: 'Allowed values: "ide", "sata", "virtio_scsi",
                            "virtio_block".'
                          type: string
                        usbController:
                          description: 'Allowed values: "nec_xhci", "piix3_uhci".'
                          type: string
                      type: object
                    type: array
                  ipv4:
                    description: 'The UUID of the IPv4 address of the server. (***NOTE:
                      The server will NOT automatically be connected to the public
                      network; to give it access to the internet, please add server
                      to the public network.)'
                    type: string
                  ipv4Ref:
                    description: Reference to a IPv4 in gridscale to populate ipv4.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  ipv4Selector:
                    description: Selector for a IPv4 in gridscale to populate ipv4.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  ipv6:
                    description: 'The UUID of the IPv6 address of the server. (***NOTE:
                      The server will NOT automatically be connected to the public
                      network; to give it access to the internet, please add server
                      to the public network.)'
                    type: string
                  ipv6Ref:
                    description: Reference to a IPv6 in gridscale to populate ipv6.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  ipv6Selector:
                    description: Selector for a IPv6 in gridscale to populate ipv6.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  isoimage:
                    description: The UUID of an ISO image in gridscale. The server
                      will automatically boot from the ISO if one was added. The UUIDs
                      of ISO images can be found in the expert panel.
                    type: string
                  labels:
                    description: |-
                      List of labels in the format [ "label1", "label2" ].
                      List of labels.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  memory:
                    description: |-
                      The amount of server memory in GB.
                      The amount of server memory in GB.
                    type: number
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters
                    type: string
                  network:
                    description: Connects a network to the server. The network ordering
                      of the server corresponds to the order of the networks in the
                      server resource block.
                    items:
                      properties:
                        bootdevice:
                          description: Make this network the boot device. This can
                            only be set for one network.
                          type: boolean
                        firewallTemplateUuid:
                          description: The UUID of firewall template.
                          type: string
                        firewallTemplateUuidRef:
                          description: Reference to a Firewall in gridscale to populate
                            firewallTemplateUuid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        firewallTemplateUuidSelector:
                          description: Selector for a Firewall in gridscale to populate
                            firewallTemplateUuid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        ip:
                          description: |-
                            Manually assign DHCP IP to the server (if applicable).
                            Manually assign DHCP IP to the server.
                          type: string
                        objectUuid:
                          description: The object UUID or id of the network.
                          type: string
                        objectUuidRef:
                          description: Reference to a Network in gridscale to populate
                            objectUuid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        objectUuidSelector:
                          description: Selector for a Network in gridscale to populate
                            objectUuid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        ordering:
                          description: DEPRECATED  Defines the ordering of the network
                            interfaces. Lower numbers have lower PCI-IDs.
                          type: number
                        rulesV4In:
                          description: Firewall template rules for inbound traffic
                            - covers ipv4 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                        rulesV4Out:
                          description: Firewall template rules for outbound traffic
                            - covers ipv4 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                        rulesV6In:
                          description: Firewall template rules for inbound traffic
                            - covers ipv6 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                        rulesV6Out:
                          description: Firewall template rules for outbound traffic
                            - covers ipv6 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  power:
                    description: |-
                      The power state of the server. Set this to true to will boot the server, false will shut it down.
                      The number of server cores.
                    type: boolean
                  storage:
                    description: |-
                      Connects a storage to the server. **NOTE: The first storage is always the boot device.
                      A list of storages attached to the server. The first storage in the list is always set as the boot storage of the server.
                    items:
                      properties:
                        objectUuid:
                          description: The object UUID or id of the storage.
                          type: string
                        objectUuidRef:
                          description: Reference to a Storage in gridscale to populate
                            objectUuid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        objectUuidSelector:
                          description: Selector for a Storage in gridscale to populate
                            objectUuid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  userDataBase64:
                    description: |-
                      For system configuration on first boot. May contain cloud-config data or shell scripting, encoded as base64 string. Supported tools are cloud-init, Cloudbase-init, and Ignition.
                      For system configuration on first boot. May contain cloud-config data or shell scripting, encoded as base64 string. Supported tools are cloud-init, Cloudbase-init, and Ignition.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.cores is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.cores)
                || (has(self.initProvider) && has(self.initProvider.cores))'
            - message: spec.forProvider.memory is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.memory)
                || (has(self.initProvider) && has(self.initProvider.memory))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ServerStatus defines the observed state of Server.
            properties:
              atProvider:
                properties:
                  autoRecovery:
                    description: |-
                      If the server should be auto-started in case of a failure (default=true).
                      If the server should be auto-started in case of a failure (default=true).
                    type: boolean
                  availabilityZone:
                    description: |-
                      Defines which Availability-Zone the Server is placed.
                      Defines which Availability-Zone the Server is placed.
                    type: string
                  changeTime:
                    description: Defines the date and time of the last object change.
                    type: string
                  consoleToken:
                    description: |-
                      The token used by the panel to open the websocket VNC connection to the server console.
                      The token used by the panel to open the websocket VNC connection to the server console.
                    type: string
                  cores:
                    description: |-
                      The number of server cores.
                      The number of server cores.
                    type: number
                  createTime:
                    description: Defines the date and time the object was initially
                      created.
                    type: string
                  currentPrice:
                    description: The price for the current period since the last bill.
                    type: number
                  hardwareProfile:
                    description: |-
                      The hardware profile of the Server. Options are default, legacy, nested, cisco_csr, sophos_utm, f5_bigip and q35 at the moment of writing. If it is not set, the backend will set it by default. Check the official docs.
                      Specifies the hardware settings for the virtual machine. Note: hardware_profile and hardware_profile_config parameters can't be used at the same time.
                    type: string
                  hardwareProfileConfig:
                    description: |-
                      Specifies the custom hardware settings for the virtual machine. Note: hardware_profile and hardware_profile_config parameters can't be used at the same time. Note: If hardware_profile_config is set, all fields of hardware_profile_config MUST be set.
                      Specifies the custom hardware settings for the virtual machine. Note: hardware_profile and hardware_profile_config parameters can't be used at the same time.
                    items:
                      properties:
                        hypervExtensions:
                          description: Boolean.
                          type: boolean
                        machinetype:
                          description: 'Allowed values: "i440fx", "q35_bios", "q35_uefi".'
                          type: string
                        nestedVirtualization:
                          description: Boolean.
                          type: boolean
                        networkModel:
                          description: 'Allowed values: "e1000", "e1000e", "virtio",
                            "vmxnet3".'
                          type: string
                        serialInterface:
                          description: Boolean.
                          type: boolean
                        serverRenice:
                          description: Boolean.
                          type: boolean
                        storageDevice:
                          description: 'Allowed values: "ide", "sata", "virtio_scsi",
                            "virtio_block".'
                          type: string
                        usbController:
                          description: 'Allowed values: "nec_xhci", "piix3_uhci".'
                          type: string
                      type: object
                    type: array
                  id:
                    description: UUID of the server.
                    type: string
                  ipv4:
                    description: 'The UUID of the IPv4 address of the server. (***NOTE:
                      The server will NOT automatically be connected to the public
                      network; to give it access to the internet, please add server
                      to the public network.)'
                    type: string
                  ipv6:
                    description: 'The UUID of the IPv6 address of the server. (***NOTE:
                      The server will NOT automatically be connected to the public
                      network; to give it access to the internet, please add server
                      to the public network.)'
                    type: string
                  isoimage:
                    description: The UUID of an ISO image in gridscale. The server
                      will automatically boot from the ISO if one was added. The UUIDs
                      of ISO images can be found in the expert panel.
                    type: string
                  labels:
                    description: |-
                      List of labels in the format [ "label1", "label2" ].
                      List of labels.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  legacy:
                    description: |-
                      Legacy-Hardware emulation instead of virtio hardware. If enabled, hot-plugging cores, memory, storage, network, etc. will not work, but the server will most likely run every x86 compatible operating system. This mode comes with a performance penalty, as emulated hardware does not benefit from the virtio driver infrastructure.
                      Legacy-Hardware emulation instead of virtio hardware. If enabled, hotplugging cores, memory, storage, network, etc. will not work, but the server will most likely run every x86 compatible operating system. This mode comes with a performance penalty, as emulated hardware does not benefit from the virtio driver infrastructure.
                    type: boolean
                  locationUuid:
                    description: |-
                      The location this server is placed. The location of a resource is determined by it's project.
                      The location this object is placed.
                    type: string
                  memory:
                    description: |-
                      The amount of server memory in GB.
                      The amount of server memory in GB.
                    type: number
                  name:
                    description: |-
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters.
                      The human-readable name of the object. It supports the full UTF-8 character set, with a maximum of 64 characters
                    type: string
                  network:
                    description: Connects a network to the server. The network ordering
                      of the server corresponds to the order of the networks in the
                      server resource block.
                    items:
                      properties:
                        autoAssignedIp:
                          description: |-
                            DHCP IP which is automatically assigned to the server (if applicable).
                            DHCP IP which is automatically assigned to the server.
                          type: string
                        bootdevice:
                          description: Make this network the boot device. This can
                            only be set for one network.
                          type: boolean
                        createTime:
                          description: Defines the date and time the object was initially
                            created.
                          type: string
                        firewallTemplateUuid:
                          description: The UUID of firewall template.
                          type: string
                        ip:
                          description: |-
                            Manually assign DHCP IP to the server (if applicable).
                            Manually assign DHCP IP to the server.
                          type: string
                        mac:
                          description: network_mac defines the MAC address of the
                            network interface.
                          type: string
                        networkType:
                          description: One of network, network_high, network_insane.
                          type: string
                        objectName:
                          description: Name of the storage.
                          type: string
                        objectUuid:
                          description: The object UUID or id of the network.
                          type: string
                        ordering:
                          description: DEPRECATED  Defines the ordering of the network
                            interfaces. Lower numbers have lower PCI-IDs.
                          type: number
                        rulesV4In:
                          description: Firewall template rules for inbound traffic
                            - covers ipv4 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                        rulesV4Out:
                          description: Firewall template rules for outbound traffic
                            - covers ipv4 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                        rulesV6In:
                          description: Firewall template rules for inbound traffic
                            - covers ipv6 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                        rulesV6Out:
                          description: Firewall template rules for outbound traffic
                            - covers ipv6 addresses.
                          items:
                            properties:
                              action:
                                description: |-
                                  This defines what the firewall will do. Either accept or drop.
                                  This defines what the firewall will do. Either accept or drop.
                                type: string
                              comment:
                                description: |-
                                  Comment.
                                  Comment.
                                type: string
                              dstCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then all IPs have access to this service.
                                type: string
                              dstPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                              order:
                                description: |-
                                  The order at which the firewall will compare packets against its rules. A packet will be compared against the first rule, it will either allow it to pass or block it and it won't be matched against any other rules. However, if it does no match the rule, then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                  The order at which the firewall will compare packets against its rules.
                                  A packet will be compared against the first rule, it will either allow it to pass or block it
                                  and it won't be matched against any other rules. However, if it does no match the rule,
                                  then it will proceed onto rule 2. Packets that do not match any rules are blocked by default (Only for inbound).
                                type: number
                              protocol:
                                description: |-
                                  Either 'udp' or 'tcp'.
                                  Either 'udp' or 'tcp'
                                type: string
                              srcCidr:
                                description: |-
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                  Either an IPv4/6 address or and IP Network in CIDR format. If this field is empty then this service has access to all IPs.
                                type: string
                              srcPort:
                                description: |-
                                  A Number between 1 and 65535, port ranges are separated by a colon for FTP.
                                  A Number between 1 and 65535, port ranges are seperated by a colon for FTP
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  power:
                    description: |-
                      The power state of the server. Set this to true to will boot the server, false will shut it down.
                      The number of server cores.
                    type: boolean
                  status:
                    description: Status indicates the status of the object.
                    type: string
                  storage:
                    description: |-
                      Connects a storage to the server. **NOTE: The first storage is always the boot device.
                      A list of storages attached to the server. The first storage in the list is always set as the boot storage of the server.
                    items:
                      properties:
                        bootdevice:
                          description: Make this network the boot device. This can
                            only be set for one network.
                          type: boolean
                        bus:
                          description: The SCSI bus id. The SCSI defines transmission
                            routes like Serial Attached SCSI (SAS), Fibre Channel
                            and iSCSI. Each SCSI device is addressed via a specific
                            number. Each SCSI bus can have multiple SCSI devices connected
                            to it.
                          type: number
                        capacity:
                          description: Capacity of the storage (GB).
                          type: number
                        controller:
                          description: Defines the SCSI controller id. The SCSI defines
                            transmission routes such as Serial Attached SCSI (SAS),
                            Fibre Channel and iSCSI.
                          type: number
                        createTime:
                          description: Defines the date and time the object was initially
                            created.
                          type: string
                        lastUsedTemplate:
                          description: Indicates the UUID of the last used template
                            on this storage (inherited from snapshots).
                          type: string
                        licenseProductNo:
                          description: If a template has been used that requires a
                            license key (e.g. Windows Servers) this shows the product_no
                            of the license (see the /prices endpoint for more details).
                          type: number
                        lun:
                          description: Is the common SCSI abbreviation of the Logical
                            Unit Number. A lun is a unique identifier for a single
                            disk or a composite of disks.
                          type: number
                        objectName:
                          description: Name of the storage.
                          type: string
                        objectUuid:
                          description: The object UUID or id of the storage.
                          type: string
                        storageType:
                          description: Indicates the speed of the storage. This may
                            be (storage, storage_high or storage_insane).
                          type: string
                        target:
                          description: Defines the SCSI target ID. The target ID is
                            a device (e.g. disk).
                          type: number
                      type: object
                    type: array
                  usageInMinutesCores:
                    description: Total minutes of cores used.
                    type: number
                  usageInMinutesMemory:
                    description: Total minutes of memory used.
                    type: number
                  userDataBase64:
                    description: |-
                      For system configuration on first boot. May contain cloud-config data or shell scripting, encoded as base64 string. Supported tools are cloud-init, Cloudbase-init, and Ignition.
                      For system configuration on first boot. May contain cloud-config data or shell scripting, encoded as base64 string. Supported tools are cloud-init, Cloudbase-init, and Ignition.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
